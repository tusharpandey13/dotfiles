\help
\du
\dt
\?
\q
\help
\c nakshatra
\d
SELECT * FROM languages;
GRANT ALL PRIVILEGES ON TABLE languages TO tushar;
GRANT ALL PRIVILEGES ON TABLE languages TO tushar;
\d+
\d+
\d+
\d+
\d+
\d+
\d+
\d+
\d+
\d+
\d+
\d+
\d+
\d+
\d+
\d+ lives
\d+ lives
\d+ lives
\d+ lives
\d+ lives
\d+
truncate transactionStatuses;
truncate transactionStatuse;
truncate transactionStatus;
\d+
TRUNCATE transactionStatuses;
DELETE FROM transactionStatuses;
DELETE FROM transactionStatus;
\dt
delete from transactionStatuses;
delete from public.transactionStatuses;
\d
\d
\d customers
\d wallets
\d wallets
\d
\d+
create table employee(int ssn, name varchar(10), dob date, address varchar(20), dno int);
create table employee(ssn int, name varchar(10), dob date, address varchar(20), dno int);
\d department
\d employee
drop table employee;
create table employee(ssn int,name varchar(10),dob data,address varchar(20),dno int);
create table(ssn int, name varchar(10), dob date,address varchar(20), dno int);
create table bc1;
create table bc1;
\d
\d+ bc1
CREATE TABLE BOOK(BID INTEGER PRIMARY KEY,TITLE VARCHAR(20),PUBYEAR VARCHAR(20),PUBNAME REFERENCES PUBLISHER(NAME) ON DELETE CASCADE);
